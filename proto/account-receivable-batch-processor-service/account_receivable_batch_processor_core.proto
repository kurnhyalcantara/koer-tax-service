syntax = "proto3";

package account_receivable_batch_processor.service.v1;

option go_package = "./pb";

enum Direction {
    UNKNOWN_DIRECTION = 0;
    DESC = 1;
    ASC = 2;
};

enum Status{
    UNKNOWN_STATUS = 0;
    PENDING_MAKER_CONFIRMATION = 1;
    ON_CHECKER = 2;
    ON_SIGNER = 3;
    ON_RELEASER = 4;
    SUCCESS = 5;
    FAILED = 6;
    RETUR = 7;
    WAITING_SCHEDULE = 8;
    REJECTED = 9;
    WAITING_PROCESS = 10;
    ON_PROGRESS = 11;
    PROCEED_NEXT_PROCESS = 12;
    APPROVED = 13;
    ACTIVE = 14;
    INACTIVE = 15;
    EXPIRED = 16;
	FILE_IN_PROCESS = 17;
	FILE_COMPLETED = 18;
	INVALID_FILE_FORMAT = 19;
	CANCELED =20;
};


enum AmountType {
    UNKNOWN_AMOUNT_TYPE = 0;
    MAXIMUM = 1;
    FIXED = 2;
};

enum MandateCategory {
    UNKNOWN_MANDATE_CATEGORY = 0;
    ON_US = 1;
    OFF_US = 2;
};

enum MandateType {
    UNKNOWN_MANDATE_TYPE = 0;
    ADHOC = 1;
    FREQUENCY = 2;
};

enum TransactionFrequency {
    UNKNOWN_TRANSACTION_FREQUENCY = 0;
    DAILY = 1;
    WEEKLY = 2;
    MONTHLY = 3;
    END_OF_MONTH = 4;
    YEARLY = 5;
};

enum BatchType {
    UNKNOWN_BATCH_TYPE = 0;
    MANDATE_MANAGEMENT = 1;
    DIRECT_DEBIT = 2;
};

enum ActionType {
    UNKNOWN_ACTION_TYPE = 0;
    APPROVE = 1;
    DELETE = 2;
    REJECT = 3;
    CANCEL = 4;
};

enum UserType {
    UNKNOWN_USER_TYPE = 0;
    BA = 1;
    CA = 2;
    CU = 3;
};
  
enum TaskStep {
    UNKNOWN_TASK_STEP = 0;
    MAKER = 1;
    CHECKER = 2;
    SIGNER = 3;
    RELEASER = 4;
    COMPLETE = 5;
};

enum FileDownloadMethod {
    UNKNOWN_FILE_DOWNLOAD_METHOD = 0;
    HTTP = 1;
    STREAM = 2;
    NON_STREAM = 3;
};

enum ActionTypeBatchProcessor {
    UNKNOWN_ACTION_TYPE_BATCH_PROCESSOR = 0;
    APPROVAL = 1;
    VALIDATOR = 2;
}

enum AmountTypeConvertToBiFast{
    UnknownAmt = 0;
    MaxAmt = 1;
    FixedAmt = 2;
}

enum TransactionFrequencyConvertToBiFast {
    UNKNOWN_TRANSACTION_FREQUENCY_BIFAST = 0;
    DAIL = 1;
    WEEK = 2;
    MNTH = 3;
    EOM = 4; // End Of Month
    YEAR = 5;
};